// Simple on/off switches for optional site features. Flip a value to false
// to disable a feature everywhere without removing its code — delete the
// feature's files and its one usage in App.jsx when you're ready to remove
// it for good.
export const FEATURES = {
  feedbackWidget: true,
};
