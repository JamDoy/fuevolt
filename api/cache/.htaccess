# Cache files here can contain a live MyFuel NT bearer token — block direct
# web access; only nt-fuel.php (same server) reads them.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order allow,deny
  Deny from all
</IfModule>
